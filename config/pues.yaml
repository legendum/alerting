# Which pues/base/ subdirectories this consumer vendors.
# Add entries here to adopt more of pues over time, then run `bun run pues`.
pues:
  - theme
  - style
  - db

db:
  path: data/alert.db

style:
  vars:
    pues-topbar-height: "72px"
